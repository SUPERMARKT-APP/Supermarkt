SELECT * FROM products_stores
INNER JOIN stores
ON products_stores.stores_id = stores.id
INNER JOIN companies
ON stores.company_id = companies.id
INNER JOIN locations
ON stores.location_id = locations.id
INNER JOIN products
ON products_stores.products_id = products.id
ORDER BY stores_id;


-- SELECT * FROM users
-- INNER JOIN users_products
-- ON users.id = users_products.users_id
-- INNER JOIN products
-- ON users_products.products_id = products.id
-- LEFT JOIN products_stores
-- ON products.id = products_stores.products_id
-- # WHERE products.name LIKE '%piña%';